"""Shared cross-domain contracts and helpers.

Submodules have been consolidated into their canonical locations:
- errors → app.core.exceptions
- schemas → app.schemas.common
- time → app.core.utils
- geo → app.utils.distance, app.utils.geo
- auth → app.api.api_v1.dependencies.auth
"""
